# 个人预算工具 PRD v0.4 · 数据层重构与算法升级

> 版本：v0.4 · 2026-09-05
> 前置：v0.3（5 tab IA + 视觉定型）已通过视觉验收
> 状态：**进入"真实可用系统"实现阶段**

---

## 1. 升级原因（用户对 v0.3 demo 的反馈）

- 太多数字硬编码，无法反映用户真实记账状态
- "记一笔"录入后页面不同步，无持久化
- "今天还能安心花"是写死的，不反映真实资金属性
- 账本日历只有 9 月，无法翻到历史/未来月份
- 侧边菜单列了 6 项但多数不能工作
- 场景 C 写死"服饰美妆分类"和固定替代文案
- 分析页图表装饰性强，无数据下钻

## 2. 七项关键变化

| # | 变更 | 说明 |
|---|---|---|
| **K1** | **数据层重构** | localStorage 单一数据源；首页/账本/预算/资产/分析全部由账单实时计算，禁止任何展示性硬编码 |
| **K2** | **真实账本** | 日历支持月切换、回到今天、点日期看当天小结（含收入/支出/净变化/账单/一句话复盘）；筛选与搜索真正可用 |
| **K3** | **记一笔 CRUD** | 支持新增/编辑/删除；日期、账户、备注、分类、类型全部可改；提交后立即联动首页、预算、资产、日历、分析 |
| **K4** | **安心花算法重定义** | 区分总资产 / 锁定储蓄 / 必须预留 / 可支配；按"距离下次补给天数"动态算 |
| **K5** | **首页信息压缩** | 分类预算首页只展示 3~4 个重点项（按余额告急度排序），完整列表放计划页；最近账单只显示 3 笔 + "查看全部" |
| **K6** | **计划可编辑** | 预算可调额度、存钱可新增和存入金额、想买可新增/删除/完成 |
| **K7** | **场景 C 真实化** | 根据商品所属分类、当前余额、是否刚需、距离补给天数动态三档判断 + 动态替代文案 |

## 3. 安心花算法（核心公式 v0.4）

```
可支配 = 总资产 - 锁定储蓄 - 必须预留
今日安心花 = floor(可支配 ÷ 距离下次补给天数)
```

**数据来源**：
- 总资产 = Σ(账户余额)
- 锁定储蓄 = Σ(kind=locked 的账户余额)，禁止进入安心花计算
- 必须预留 = 用户在"补给与消费规则"里设定的 reserveAmount
- 距离下次补给天数 = (nextSupplyDate - 今天)

**点击"今天还能安心花"** → 模态"计算详情"，展示该数字的逐项组成 + 解释，**不再跳资产页**。

**"当前余额"卡片** → 点击进入"资产详情"（账户列表 + 合计）。

## 4. 账户资金属性

每账户三选一：

| kind | 含义 | 是否进入安心花 |
|---|---|---|
| `daily` | 日常可用 | ✅ 进入 |
| `buffer` | 备用可动（应急） | ✅ 进入（但提醒谨慎用） |
| `locked` | 锁定储蓄（农村商/定期/理财） | ❌ 不进入 |

## 5. 补给与消费规则

用户在抽屉"补给与消费规则"里设置：

| 字段 | 含义 | 默认 |
|---|---|---|
| `nextSupplyDate` | 下一次允许补给日期 | 30 天后 |
| `supplyAmount` | 计划补给金额 | 1500 |
| `reserveAmount` | 必须预留生活费 | 100 |

## 6. 场景 C 三档判断

| 档 | 触发条件 | 文案 |
|---|---|---|
| 🔴 暂不建议 | 买完某分类超支 / 可支配撑不到补给 / 锁定资金被侵蚀 | 给出剩余缺口 + 替代方案 |
| 🟡 建议等等 | 买完可支配低于 1.5 倍预留 / 比例超过余额 30% | 给出"买完日均剩多少" |
| 🟢 可以买 | 都不踩 | 给出"买完各分类剩余" |

**判断必须基于商品金额、分类预算、距离补给时间、可支配余额、必须预留、锁定资金**。
**替代建议按商品分类动态生成**：服饰类推闲鱼/二手；数码类推延后/分期；食品类推平替；等等。

## 8. 侧边菜单精简为 4 项

| 顺序 | 名称 | 职责 |
|---|---|---|
| 1 | 账户与资金属性 | 管理账户余额和 kind |
| 2 | 分类管理 | 增删分类、调整额度、改 emoji |
| 3 | 补给与消费规则 | 编辑补给日期、金额、预留 |
| 4 | 数据备份恢复 | 导出 JSON、导入 JSON |

**移除**：记账提醒（无法工作）、外观设置（暂不做）、个人设置（泛化）

## 9. 首页信息层级（v0.4 压缩版）

1. 问候 + 时段切换（今日/本周/本月）
2. **主余额卡"今天还能安心花 ¥XX"**（点击 → 计算详情）
3. 三个小卡（今日花了 / 今日挣了 / 本月已存 ¥）
4. 快捷入口：🤔 想买 / 🌙 今日复盘
5. **最近 3 笔账单**（不是 3 日，是 3 笔）+ "查看全部 >"
6. **重点分类 3~4 个**（按"今日可花"接近 0 / 超支排序）
7. **强制储蓄本月进度**（不再叫"今日储蓄"）

**完整分类列表** → 计划 → 预算 tab
**完整账单** → 账本 tab

## 10. 分析页：只做三类有数据支撑的可视化

1. **分类消费占比与排行**（横条 + 排行表，点击下钻到该分类的全部账单）
2. **日/周/月消费趋势**（30 天柱状图，点击某天跳账本日历）
3. **收入支出与预算执行对比**（柱状对比，点击下钻）

**禁止**无决策价值的装饰图表。

## 11. 产品哲学（用户明确强调）

> **核心目标不是让用户少花钱，而是让用户知道"钱应该先花在哪里"，避免在现金不足时进行不必要的透支消费。**

所有 UI 文案围绕"花对地方"，禁止说教式"省着点"。

## 12. 里程碑

| 阶段 | 内容 |
|---|---|
| M0 | PRD v0.4 定稿 ✅ |
| **M1** | 数据层 + 计算函数 + 持久化（localStorage） |
| **M2** | 安心花算法重写 + 计算详情模态 + 资金属性设置 |
| **M3** | 真实账本（多月切换 + 筛选 + 当日详情 + 自动复盘） |
| **M4** | 记一笔 CRUD + 编辑模态 |
| **M5** | 首页压缩 + 场景 C 三档 |
| **M6** | 计划可编辑 + 分析三类图表 |

---

## 13. 变更日志

- **v0.4（2026-09-05 下午）**：数据层重构、安心花算法重定义、账户资金属性、补给周期、场景 C 三档、首页压缩、计划可编辑、分析三类真实图表
- **v0.3（2026-09-05 上午）**：5 tab IA、视觉定型
- **v0.2（2026-09-04）**：12 项决策
- **v0.1（2026-08-31）**：首版 4 项决策