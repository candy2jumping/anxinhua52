# 个人预算工具 PRD v0.2

> 项目代号：easybudget（占位名，可改）
> 版本：v0.2 需求收敛稿 · 2026-09-04
> 状态：**12 项决策已锁，3 项待定，未开工实现**
> 前置：v0.1（2026-08-31）首次收敛 → 用户补充设备信息、历史账本、消费场景后升级到 v0.2

---

## 1. 一句话定义

**不是记账 App，是一个每天告诉你「今天还能安心花多少」的额度显示器。**

记账只是手段，目的是把"我不敢花钱，因为心里没底"换成"看了数字就敢做决定"。

更直接的一句话：**给每一笔钱一个去处，给每一次花钱一个底气。**

---

## 2. 用户与场景

### 2.1 用户画像

- 在校大学生（华为 nova SE 11，HarmonyOS NEXT 系统）
- 生活费由父母不定期补给——"一旦没钱或只剩 100 多就补"，**不是固定发薪日**
- 关注"自我提升"，已有多种副业收入渠道（代课、勤工俭学、PPT/推文/排版接单、家教、自媒体等）
- 过去用钱迹 App 做过预算框架但因操作繁琐未坚持
- 真实痛点：**不敢改善生活品质**，想买衣服/想体验都因心里没数而放弃
- 设备约束：仅手机端，电脑端后续

### 2.2 三个核心场景

| 编号 | 场景 | 用户期望 | 设计要求 |
|---|---|---|---|
| **S1** 食堂窗口 | 付完饭钱走出十几步路 | 掏出手机直接点悬浮窗/主屏入口 → 输金额 → 选分类 → 完事。时间、支付方式等自动记录，不让填任何额外字段 | **3 秒完成**；自动记录当下时间、默认账户 |
| **S2** 晚上躺床上 | 一笔笔回想当天花了什么 | 一天结束后翻一下，对今天花销有数，知道有没有超、省了多少、挣了多少；同时支持周/月度复盘 | 日报：金额+分类汇总；周报：日均、最高一天、超支项；月报：结余多少、哪格异常 |
| **S3** 站在那儿纠结 | 看上 200 块的泳衣或 200 块的衣服 | 手机告诉我——**该不该买，结合我真实的收支，不是瞎说**；如果想买，给替代方案 | 输入"想买"金额 → 系统基于本月剩余、该分类余额、未来固定支出给出建议；可走可买可缓 |

### 2.3 明确不做的场景

- 多人共享账本、AA 分摊
- 理财、投资、基金、股票
- 复杂报表、图表分析（v1 只给结论性文字）
- 自动 AI 分类（v1 手动选分类，规则化匹配即可）
- AI 自动生成"该不该买"的文字回复（v1 用规则引擎 + 模板字符串，LLM 后续再加）

---

## 3. 已锁定的核心决策（v0.2 共 12 项）

来源：v0.1 讨论 4 项 + v0.2 用户补充 8 项。

| 编号 | 决策项 | 结论 | 替代/权衡 |
|---|---|---|---|
| **D1** | 平台形态 | **v1 仅手机端**；电脑端后续再加 | 用户已明确："先在手机上能运行"；电脑端等 v2 |
| **D2** | 记录方式 | **纯手动秒记**：金额 + 分类，其他字段全砍 | 摩擦最小；漏记率高的解法见 D11 |
| **D3** | 预算模型 | **总额度 + 分类信封**，每格独立看剩余 | 解决"衣物不敢买"—— 衣物那格单独有钱、单独看 |
| **D4** | 额度算法 | **按分类分别算**今日可花，不是一个总数 | 主屏每行有自己的"今天还能花多少" |
| **D5** | 系统平台 | **鸿蒙 HarmonyOS NEXT**（纯血鸿蒙） | 不能装 APK，不能套 Android 套壳；PWA 加入主屏可用 |
| **D6** | 入口形态 | **点击主屏图标一点直达输入页**（PWA 模式） | iOS 第三方悬浮窗不允许；鸿蒙方案见 D5；这一步摩擦比"原生悬浮球"多约 1 秒，可接受 |
| **D7** | 生活费到账 | **不定额不定时**，爸妈看到余额少就补 | **预算周期不按自然月重置，改按"当前余额"模型**：余额随时记录父母的补给，预算按"这个月手里有多少钱"算 |
| **D8** | 余额结转 | **是，结转**，这个月没花完下个月接着花 | 鼓励"少花就多剩"，让储蓄自然积累 |
| **D9** | 不设限支出 | **所有支出都设上限** | 即便意外支出也设个"安全垫" |
| **D10** | 收入记录 | **要**，必须支持 | v1 必做；分类：固定收入、生活费补给、勤工俭学、兼职/接单、家庭支持、其他 |
| **D11** | 漏记折扣 | **默认 0%**，用满一个月后自校准 | 解 D2 与 D4 的核心冲突；详情见第 5 节 |
| **D12** | 复盘维度 | **日复盘 + 周复盘 + 月复盘**，季度不做 | S2 用户明确提出；按时间窗生成结论性文字 |

---

## 4. 待定项（3 项，按需补齐）

| 编号 | 问题 | 默认方案 |
|---|---|---|
| P1 | 强制储蓄的"目标"是应急基金？还是具体想买的东西（如 iPad）？ | v1 仅做"每月先存下 100"，可选目标名；具体目标 v2 |
| P2 | 后端方案：Supabase（省事、第三方）还是自建（可控、要运维）？ | **建议 Supabase**：你的财务数据敏感性是中等，且 Supabase 的免费层够单人用；可随时迁出 |
| P3 | 多端同步需要做吗？现在只有手机，是否先纯本地？ | **建议先 Supabase + 登录**，成本极低；纯本地后续要加多端时再迁数据麻烦 |

---

## 5. 核心机制

### 5.1 预算模型：当前余额制

不像传统月度预算那样每月 1 号清零重置，本工具跟踪的是**当前手里还有多少钱**：

```
当前可用余额 = 上次父母补给金额 + 期间所有收入 - 期间所有支出
```

每次父母打钱进来 = **"补给事件"**，触发"本月可用总额"刷新。

### 5.2 今日可花算法（按分类）

```
某分类今日可花 = max(
  0,
  floor(
    (该分类本月额度 - 该分类本月已花) × (1 - 漏记折扣) ÷ 剩余天数
  )
)
```

- 剩余天数：当前周期剩余天数（由补给节奏决定，v1 先按 30 天简化）
- **向下取整到元**：宁可算少不可算多
- 已超支 → 显示 ¥0 + 红色提示

### 5.3 漏记折扣（关键解法）

**为什么必须有**：D2 选纯手动，必然漏记。漏记让"今日可花"虚高，用户信了它就真超支——本产品最严重的失效模式。

**做法**：设置页一个滑块「我大概会漏记 _%」参与 5.2 的计算。
- 默认 0%（先不设）
- 用满一个周期后，根据"已记录 vs 实际钱包消耗"反推建议值（用户手动输入本月实际剩余即可校准）
- **原则：宁可额度算少，不可算多**

### 5.4 收入与补给

每次进账 → 用户**手动记一笔收入流水**：
- 类型：父母补给 / 勤工俭学 / 兼职接单 / 家庭支持 / 其他
- 金额
- 备注（可空）
- 触发"当前可用余额"刷新 + 自动建议要不要补到强制储蓄

---

## 6. 需求清单（MoSCoW）

### Must have（v1 必做，缺了产品不成立）

| 编号 | 需求 | 来源 |
|---|---|---|
| M1 | 快速记账：金额 + 分类，其他全砍 | D2, S1 |
| M2 | 当前余额制 + 父母补给事件 | D7 |
| M3 | 分类信封预算（13 个分类，见第 7 节） | D3, S1 |
| M4 | 主屏显示：当前余额 + 每个分类的"今日可花"与进度条 | S1, S2 |
| M5 | 分类记忆：上次用的置顶 | S1 |
| M6 | 收入记录（含补给事件） | D10 |
| M7 | 漏记折扣设置 | D11 |
| M8 | 日复盘：今日花了多少、剩多少、挣了多少 | S2, D12 |
| M9 | 周复盘：日均、最高一天、超支项 | S2, D12 |
| M10 | 月复盘：结余多少、哪格异常、下月调整建议 | S2, D12 |
| M11 | PWA 添加到主屏 | D5, D6 |
| M12 | 强制储蓄：每月先存 100，独立分类 | 预算表 B 节 |
| M13 | 跨端同步 + 登录（Supabase，参照 P2） | P3 |

### Should have（v1 应有，影响体验完整度）

| 编号 | 需求 |
|---|---|
| S1 | 场景 C 智能建议："想买某物"输入金额 → 系统基于本月剩余 + 该分类余额 + 固定支出给出可走/可买/可缓结论 |
| S2 | 大额确认提示：单笔超过该分类额度的 20% 时弹出"衣物还剩 ¥300，确定吗"，**只提示不阻止** |
| S3 | 分类额度调剂：本月内 A 分类余额挪到 B 分类，并记录调剂流水 |
| S4 | 漏记率自校准：月度复盘时反推建议折扣值并提示 |
| S5 | 撤销/删除：刚记错的一笔 3 秒内可撤销 |
| S6 | 周期切换：当父母补给进来时，提示"开启新周期吗" |
| S7 | 替代方案提示（场景 C 配套）：不建议买时给出"二手平台 / 延时 / 替代品"模板提示 |

### Could have（v2 再说）

| 编号 | 需求 |
|---|---|
| C1 | 电脑端响应式版本 |
| C2 | 攒钱目标（想买 iPad，每月自动划走） |
| C3 | 周期性固定支出自动入账（房租、话费） |
| C4 | 账单导入做月末对账 |
| C5 | 离线优先（弱网/无网也能记） |
| C6 | 数据导出 CSV |

### Won't have（明确不做）

多人共享、理财投资、AI 自动分类、复杂图表、社交分享、自动通知。

---

## 7. 分类体系（来源于钱迹账本，比手写图更全）

### 7.1 支出 13 类（按 A/B/C/D 分组）

**A. 核心生活 1070 元**
| 分类 | 额度 | 说明 |
|---|---|---|
| 餐饮伙食 | 720 | 食堂、外卖、食材、水果；6+10+8=24 天 × 30 元 |
| 交通 | 100 | 公交地铁、打车、共享单车、寒暑假火车票 |
| 话费网费 | 50 | 手机话费、宿舍宽带 |
| 学习用品书籍 | 100 | 教材、专业书、打印复印、文具 |
| 日用品 | 100 | 洗漱、纸巾、洗衣液、电费等日耗 |

**B. 未来投资 200 元**
| 分类 | 额度 | 说明 |
|---|---|---|
| 自我提升 | 50 | 付费课程、考证、软件会员 |
| 健康管理 | 50 | 健身、装备、药品、体检 |
| **强制储蓄** | 100 | **每月雷打不动先存**，独立目标，可积累 |

**C. 品质享受 270 元**
| 分类 | 额度 | 说明 |
|---|---|---|
| 零食饮品 | 50 | 咖啡、饮料；要求养生一点 |
| 社交娱乐 | 20 | 朋友聚餐、生日礼物、电影 |
| 服饰美妆 | 100 | 衣服、鞋、护肤、化妆、配饰 |
| 旅行体验 | 100 | 短途旅行、展览 |

**D. 其他杂项 20 元**
| 分类 | 额度 | 说明 |
|---|---|---|
| 意外支出 | 20 | 维修、罚款、医疗门诊 |

**支出合计 1560**，**目标结余 340 元**（来源：固定收入 1900 - 支出 1560 = 340）

### 7.2 收入 6 类

| 分类 | 说明 |
|---|---|
| 父母补给 | **触发新周期的核心事件** |
| 生活费 | 类似"父母补给"，按实际打款形式记 |
| 勤工俭学 | 学校岗位 |
| 兼职接单 | 代课、家教、PPT/推文/排版等 |
| 家庭支持 | 亲戚转账、生日红包等 |
| 其他收入 | 退费、奖金、返还等 |

---

## 8. 关键交互流程

### 8.1 记一笔支出（S1，目标 3 秒）

```
打开应用（从主屏图标）
  → 数字键盘已展开，光标已在金额输入框
  → 输入金额（默认无小数点，按整数记）
  → 点分类（上次用的排第一）
  → 立即完成，底部 toast：「餐饮 -¥18，今日还能吃 ¥24」+ 撤销按钮
```

不允许出现的中间步骤：选日期、选账户、选商家、写备注、二次确认。

时间默认当前；账户默认微信（用户用微信支付为主）；备注折叠到长按弹出。

### 8.2 记一笔收入 / 补给（D10）

```
点"+" → 切到「收入」标签
  → 数字键盘 → 输入金额
  → 选收入类型（父母补给排第一）
  → 完成
  → 系统提示：「补给 ¥1500，是否开启新周期？」→ 是/否
```

### 8.3 场景 C 智能建议（想买东西时）

```
主屏顶部有一个输入框："想买什么？输入金额看看"
  → 输入 200
  → 系统根据：
    - 当前可用余额（如果买了还剩多少）
    - 服饰美妆分类余额（买了这格还剩多少）
    - 已花的本月剩余天数（剩下的日子还能不能正常花）
    - 强制储蓄目标（本月储蓄进度）
  → 输出三类结论：
    · 「可以买」：所有维度都宽裕
    · 「可以缓」：买了虽然能花但日子紧
    · 「不建议」：买了某格就空了
  → 附带一句"如果是社交场合需要新衣，二手平台几十块的也能穿"等模板建议
```

### 8.4 复盘（S2）

**日复盘（晚 22:00 推送，也可手动打开）**：
- 今天花了多少 → 分类汇总
- 今天挣了多少 → 收入汇总
- 净变化：结余多少

**周复盘（周日推送）**：
- 这周日均多少 → 同比上周
- 哪一天超支
- 这周有没有未用的分类

**月复盘（月末推送）**：
- 本月总收入、总支出、净结余
- 各分类 vs 预算：超支项 + 节省项
- 强制储蓄完成度
- 下月建议调整（基于本月异常项）

---

## 9. 数据模型

```
user（用户，Supabase 自带）
  id, email, created_at

balance_event（余额事件 / 补给事件）
  id, user_id, occurred_at, amount, source（父母补给/生活费/勤工俭学/兼职/家庭支持/其他）
  note

category（分类）
  id, user_id, name, monthly_limit, sort_order, color, group（A/B/C/D/收入）, is_income

transaction（流水）
  id, user_id, amount_cents, category_id, occurred_at, note
  is_income（false=支出 true=收入）
  created_at（录入时间，用于漏记率反推）

setting（设置）
  user_id, cycle_days（默认 30）, miss_rate（默认 0）, large_txn_threshold（默认 0.2）, large_txn_enabled
```

**金额单位一律为「分」（整数）**——所有记账类 App 的硬性要求。

---

## 10. 鸿蒙 HarmonyOS NEXT 平台约束（重要！）

鸿蒙 NEXT 是华为纯血鸿蒙，**与 Android/iOS 完全不同**：

| 能力 | 安卓 | iOS | **鸿蒙 NEXT** |
|---|---|---|---|
| 第三方悬浮窗 | 可 | 不行 | **不行**（仅系统应用可） |
| 桌面小组件 | 可 | 可 | **有限支持**（需原生 ArkTS） |
| 通知栏快捷 | 可 | 有限 | **不行**（需原生） |
| PWA 添加主屏 | 可 | 可 | **可**，体验接近 Chrome |
| 后台服务 | 可 | 不可 | 不可 |

**结论**：v1 走 **PWA 添加到主屏 → 一点直达输入页**，这套鸿蒙 NEXT 支持良好。后续若发现坚持不下来或想要小组件，再考虑 ArkTS 原生（学习成本高，不建议 v1）。

---

## 11. 技术选型建议

| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | React + Vite + Tailwind | 生态成熟、PWA 工具齐全 |
| 后端 | **Supabase（推荐）** | Postgres + 认证 + 实时同步，免费额度够 |
| 部署 | Vercel 或 Cloudflare Pages | 与 Supabase 配合最顺 |
| 数据 | 全部金额以「分」存整数 | 避免浮点误差 |

> 如果对数据放在 Supabase 有担心：可改为自建 FastAPI + SQLite（你本身在学 FastAPI），但要多花 2-3 天做部署、HTTPS、备份。v1 推荐先用 Supabase 出活。

---

## 12. 里程碑

| 阶段 | 内容 | 验收 |
|---|---|---|
| **M0** | 确认本文档，补齐 3 项待定 | PRD v1.0 定稿 |
| **M1** | 数据模型 + 后端 + 「记一笔支出」 | 能在手机上记并能查询 |
| **M2** | 主屏额度显示 + 今日可花算法 + 收入/补给 | **产品价值第一次成立** |
| **M3** | 日/周/月复盘 + 漏记折扣 + 大额提示 | 完整闭环 |
| **M4** | 场景 C 智能建议 + PWA 主屏入口 | v1 完成 |

---

## 13. 验收标准

1. 记一笔：实测 **≤ 3 秒**
2. 主屏**不用滚动**看到所有分类的今日可花（13 个分类需重新设计主屏布局，比如分两列网格或折叠次要项）
3. 父母补给一次进来 → 当前余额正确刷新 + 提示开新周期
4. 月底系统能告诉你"哪格超了、哪格省了、净结余多少"
5. **场景 C 实测**：输入 200 → 在不同余额状态下得到不同建议（可以买 / 可缓 / 不建议）
6. **唯一真正的成功指标**：连续用满 3 个月，实际月度净支出没超过 1900 块（生活费 + 副业总收入）

---

## 14. 变更日志

- **v0.2（2026-09-04）**：12 项决策全部锁定。补充 D5/D6（鸿蒙 + 主屏图标）、D7（不定时补给改余额制）、D8（结转）、D10（收入记录）、D12（日/周/月复盘）。加入钱迹账本分类体系（13 个支出 + 6 个收入）。
- **v0.1（2026-08-31）**：首版 4 项决策（响应式跨端、按分类算、纯手动、总额度+信封）。

---

*直接改文件，改完告诉我哪条改了。*