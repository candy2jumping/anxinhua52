# 安心花 · 手机真机实测教程（华为 nova SE 11 / HarmonyOS NEXT）

> 目标：把你电脑上这个「安心花」网页，搬到手机上真正用一遍，并跑完 P2 真机验证
> （IndexedDB 持久化 / Service Worker 离线 / 导出清空导入）。
> 本教程按「零基础、一步步照做」写，看不懂的词下面都有解释。

---

## 〇、开始前先确认两件事（30 秒）

1. **电脑和手机连的是同一个 WiFi。**
   电脑现在连的是 `CQUET`（重庆邮电大学的无线）。
   → 手机也连 `CQUET`。

2. **电脑上的服务器开着。**
   如果你之前双击过 `easybudget/start-server.bat`，会有一个小黑窗口停在那儿，
   标题类似「安心花 本地预览服务器」。关掉它就停了，要测就得开着。
   → 没开？去 `easybudget` 文件夹**双击 `start-server.bat`** 就行。

---

## 一、找到电脑在 WiFi 下的「门牌号」（IP 地址）

手机要访问电脑，得知道电脑的 IP。方法如下（**每次连之前都建议查一下，因为 IP 会变**）：

1. 电脑上：点「开始」菜单 → 输 `cmd` → 回车，打开黑色命令行窗口。
2. 输入：`ipconfig` → 回车。
3. 在刷出来的一大堆字里，找一行带 **「无线局域网适配器 WLAN」** 的段落，
   下面有一行 **「IPv4 地址 . . . . . . . . . . . . : 172.21.27.192」**。
4. 这一串 `172.21.27.192` 就是电脑现在的门牌号。

> 本次会话（2026-09-05）查到的地址是 `172.21.27.192`。
> **但如果电脑重连过 WiFi、换了网络，这串数字会变**，所以照上面步骤自己查最稳妥。

---

## 二、手机上打开安心花

1. 手机打开 **「华为浏览器」**（系统自带，图标一般是蓝白/彩色小球）。
2. 点最上面的**地址栏**，清空里面原有的字。
3. 输入（注意全是英文半角，别打错）：
   ```
   http://172.21.27.192:8080
   ```
   - 开头是 `http`（不是 https）
   - IP 是你自己查到的那串，不是抄我的
   - 末尾 `:8080` 是端口号，**冒号别漏**
4. 点「前往 / 回车」→ 正常会看到安心花首页（米白底、绿色调、顶部有时间和 ¥ 余额）。

**如果打不开，直接跳到本文第五节「常见问题」排查。**

---

## 三、把它变成桌面 App（PWA，推荐但非必须）

这样以后像点普通 App 一样点开，而且能离线用（P2 测试 B 项需要）。

1. 在华为浏览器里，点右上角或右下角的 **「⋯」菜单**（三个点 / 更多）。
2. 在菜单里找 **「添加到主屏幕」** 或 **「添加至桌面」**。
3. 名字默认「安心花」，可以改也可以不改 → 点「添加」。
4. 回到手机桌面，就能看到「安心花」图标了，点它就像开 App。

> 如果菜单里**没有**「添加到主屏幕」这个选项：
> 别慌，用浏览器**书签**也行（菜单里「收藏 / 加书签」），功能完全一样，
> 只是不会脱离浏览器。P2 的 A、C 两项照样能测，只有 B（离线）会弱一点。

---

## 四、P2 真机验证清单（重点！逐项照做）

下面三项 jsdom 测不了（手机/真浏览器才有），需要你亲手走一遍。
**每一项做完，看「预期结果」，符合就是通过；不符合就截图发我。**

### ✅ A. 数据真的存进手机了（IndexedDB 持久化）

步骤：
1. 首启会弹**引导**（4 步：①建账户 ②设月度预算 ③补给规则 ④存钱目标可跳过）。
   - 嫌麻烦可以一路点「下一步 / 跳过」，先把流程走通。
2. 进「➕ 记一笔」，记一笔：选「支出 - 餐饮」，金额 `18`，点提交。
3. **彻底关掉浏览器**：从手机底部上滑进多任务，把浏览器卡片划掉。
4. 重新打开安心花（桌面图标或书签都行）。
5. 看首页 / 账本，那笔「餐饮 -18」还在不在。

**预期结果**：还在 → ✅ 通过（数据存在手机本地数据库，不是临时内存）。
**异常**：没了 → ❌ 告诉我，说明 IndexedDB 在这个浏览器没生效。

---

### ✅ B. 断网也能开（Service Worker 离线缓存）

前提：已经「添加到主屏幕」成为独立 App，或至少用浏览器打开过一次（让缓存生效）。

步骤：
1. 先正常打开安心花一次（确保最新数据加载过）。
2. 手机开**飞行模式**（或关掉 WiFi + 关掉数据）。
3. 再打开安心花（从桌面图标，或从浏览器重新进）。
4. 看能不能打开、能不能看到之前记的账。

**预期结果**：能打开、能看到已记的账 → ✅ 通过（Service Worker 把页面缓存了）。
**异常**：白屏 / 打不开 → ❌ 告诉我。
> 注：即使 B 不通过也别怕，你的数据其实还在手机里（A 项验证过），
> 联网后照常能用，只是「离线开」这个增强没生效。

---

### ✅ C. 导出 → 清空 → 导入（端到端备份恢复）

步骤：
1. 进 **☰（左上角菜单）→ 数据备份与恢复**。
2. 点 **「导出」** → 浏览器会下载一个 `anxinhua-backup-日期.json` 文件。
   - 留意通知栏或「下载」目录，确认文件下来了。
3. 点 **「清空数据」** → 弹窗确认 → 确认。
   - 预期：所有账目、账户瞬间清空，首页变回「空状态」。
4. 再点 **「导入」** → 选刚才下载的那个 `.json` 文件 → 确认。
5. 看账目是不是**全部回来、和清空前一模一样**。

**预期结果**：清空后为空，导入后完全复原 → ✅ 通过。
**异常**：导入报错 / 数据对不上 / 少了某笔 → ❌ 截图 + 描述哪步出问题，发我。

---

## 五、常见问题（打不开先看这）

**Q1：手机浏览器显示「无法访问 / 打不开网页」**
- 确认手机和电脑连的是**同一个 WiFi**（都是 CQUET）。
- 确认电脑那个黑窗口（服务器）没关。
- 确认 IP 没抄错、端口 `:8080` 没漏。
- IP 变了？回「第一节」重新 `ipconfig` 查。

**Q2：连不上，怀疑是校园网不让设备互访**
有些校园/公司 WiFi 会禁止手机和电脑互相访问（叫「AP 隔离」）。
最稳的解决办法——**让手机开热点，电脑连手机**：
1. 手机：设置 → 移动网络 → 个人热点 → 打开，设个简单密码。
2. 电脑：连这个手机热点（不再连 CQUET）。
3. 电脑重新 `ipconfig` 查 IP（这回是手机热点给的，通常是 `192.168.x.x`）。
4. 手机浏览器用这个新 IP 访问。
   因为手机是热点主人，设备互访不受限，基本都能通。
> 反过来也行：电脑开热点、手机连电脑。 whichever 方便。

**Q3：端口 8080 被占用了**
改 `start-server.bat` 里的 `8080` 换成别的（比如 `9000`），
手机地址也相应改成 `:9000`。

**Q4：HarmonyOS NEXT 没有「添加到主屏幕」**
用浏览器书签代替（见第三节说明），功能不受影响。

---

## 六、用完怎么收尾

- 不想测了：关掉电脑上那个黑窗口（服务器停），或直接关电脑。
- 下次要测：双击 `start-server.bat` 重启，手机照本文重连。
- 你的记账数据**只存在你自己手机里**，不会上传到任何服务器，放心。

---

## 附：P2 结果速查表（跑完填给我）

| 项目 | 操作 | 预期 | 你的结果 |
|------|------|------|----------|
| A 持久化 | 记一笔→关浏览器→重开 | 数据还在 | □通过 □异常 |
| B 离线 | 开飞行模式→重开 | 能开能看账 | □通过 □异常 |
| C 备份 | 导出→清空→导入 | 完全复原 | □通过 □异常 |

三项都打「通过」→ P2 验收完成，安心花可以长期用了 🎉
有任意一项异常 → 把现象截图发我，我针对性修。
