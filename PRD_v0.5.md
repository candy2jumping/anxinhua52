# 个人预算工具 PRD v0.5 · UI 修复与 PWA 适配

> 版本：v0.5 · 2026-09-05
> 前置：v0.4 数据层 + 算法完成
> 状态：**5 项 UI 修复 + 1 项 PWA 弹窗适配**

---

## 1. 修复列表（用户试跑 v0.4 后的反馈）

| # | 问题 | 修复 |
|---|---|---|
| **F1** | 主页"最近账单"仍是 v0.4 简化版（3 笔无分组），用户要改回 v0.3 风格（按日期分组显示"近3日账单"） | 改回按日期分组，每日期下完整明细 |
| **F2** | 账本筛选器（全部 / 服饰美妆）是点击循环切换，用户不知道下一项会切到什么 | 改为**下拉菜单**：点击展开看所有选项 |
| **F3** | 日历页必须点击某日才看到当天详情，**默认日历下方应直接显示"今天"的小卡**（支出/收入/一句话） | 日历渲染后默认显示今天小卡，点击其他日期才切换 |
| **F4** | 计划 tab 切换时，三个板块虽然隐藏了但 DOM 里都还在，用户希望切换时只剩当前选中板块 | 切换时彻底重渲染，只保留当前 tab 内容 |
| **F5** | **浏览器原生弹窗 alert/prompt/confirm 在 PWA 部署到手机后会失效或样式崩** | 全部改用应用内 **appModal / appToast** |

---

## 2. 关键提醒（用户明确强调）

> "记住，我们不是在做小玩具，我们是在真正的做一个可以用的工具。"

这是产品哲学层面的表态。所有 UI 决策都要按"会部署到手机主屏、当成日常工具用"的标准来做。

**禁止**的写法：
- 浏览器原生 alert() / prompt() / confirm()
- 浏览器原生 select（移动端体验差）
- 浏览器原生日期选择器（样式不可控）

**鼓励**的写法：
- 应用内模态（统一视觉风格）
- 应用内 toast（顶部/底部短暂提示）
- 自定义 dropdown（点击展开的菜单）
- 应用内日期选择（输入框 + 简易日历或纯文本输入）

---

## 3. 应用内弹窗规范

### 3.1 appPrompt（输入对话框）
- 标题 + 单字段输入 + 取消/确定
- 自动聚焦输入框，回车确认，Esc 取消
- 返回用户输入的字符串（取消返回 null）

### 3.2 appConfirm（确认对话框）
- 标题 + 内容文字 + 取消/确定
- 返回布尔值

### 3.3 appToast（顶部提示）
- 自动消失，2 秒
- 三种类型：info（默认）/ success（绿）/ error（红）

### 3.4 appFormPrompt（多字段表单，可选）
- 标题 + 多字段 + 取消/确定
- 用于一次性需要多个输入的场景（如新增存钱目标）

---

## 4. 弹窗外其他 PWA 适配

| 场景 | 方案 |
|---|---|
| 选择账户 | 下拉菜单（不在状态栏弹系统选择器） |
| 选择日期 | 文本输入 YYYY-MM-DD（带格式校验） |
| 选择分类（筛选） | 下拉菜单 |
| 选择收入类型 | 类型 tab + 分类网格（已有） |
| 编辑账户/分类/目标 | 应用内 appFormPrompt |

---

## 5. 修复后的交互流程对照

### F1：主页"近3日账单"模块
```
标题: 近3日账单                 查看全部 ›
[9月5日 周五]                  支出 ¥127
  🍴 餐饮伙食·午饭        12:07    -¥30
  🚌 交通·地铁            14:21    -¥4
  ...
[9月4日 周四]                  支出 ¥63
  ...
```

### F2：账本筛选下拉
```
明细 ｜ 日历
[🔍 搜索备注或分类]
[全部 ▾]  [全部分类 ▾]    ← 点击展开下拉
   ↓
   ┌──────────┐
   │ ● 全部    │
   │   支出    │
   │   收入    │
   └──────────┘
```

### F3：日历下方"今天"小卡
```
日历 [2026年9月] [今]
┌────────────────┐
│ 日 一 二 三 ... │
│ ...            │
└────────────────┘

9月5日 周五 · 今天         支 ¥127 · 收 ¥0
今天餐饮花了 ¥88，主要在食堂...
🍴 餐饮·午饭    12:07    -¥30
🚌 交通·地铁    14:21    -¥4
🍴 餐饮·晚饭    19:30    -¥25
展开完整账单 ›
```

### F4：计划 tab 真正切换
```
预算 │ 存钱 │ 想买
  ↓
  渲染"预算"HTML（仅预算板块 DOM）
  ↓ 点击"存钱"
  整个容器 innerHTML 被替换（无残留 DOM）
```

---

## 6. 变更日志

- **v0.5（2026-09-05 下午）**：5 项 UI 修复 + PWA 弹窗适配
- **v0.4（2026-09-05 下午）**：数据层重构 + 算法升级
- **v0.3（2026-09-05 上午）**：5 tab IA + 视觉定型
- **v0.2（2026-09-04）**：12 项决策
- **v0.1（2026-08-31）**：首版 4 项决策