# 安心花 部署到 GitHub Pages（一步步，零基础也能做）

## 你只需做 3 件事
1. 在 GitHub 网页新建一个空仓库
2. 在 **Git Bash** 里跑 2 条命令（会弹浏览器让你登录）
3. 在仓库 Settings 里开一下 Pages

做完后，手机用固定公网网址 `https://candy2jumping.github.io/anxinhua52/` 打开，
**电脑关了也不影响，24 小时能开；首次加载后断网也能开**。

---

## 第 1 步：新建空仓库（GitHub 网页）
1. 打开 https://github.com → 右上角 「+」 → **New repository**
2. Repository name 填：`anxinhua52`
3. 选 **Public**（⚠️ 重要：GitHub 免费版只支持「公开仓库」开 Pages；私有仓库要开 Pages 必须升级 GitHub Pro/$4 月。本应用数据全在手机本地、仓库里无任何记账内容，公开仅暴露 App 代码，隐私无影响，故选 Public 最省事免费）
4. **不要**勾选 Add README / .gitignore / License（保持空仓库）
5. 点 **Create repository**

> 想要更干净的根域名（无子路径，PWA 最稳）？把仓库名改成 `candy2jumping.github.io`，
> 网址就变成 `https://candy2jumping.github.io/`。二选一即可，本指南按 `anxinhua52` 写。

---

## 第 2 步：推送代码（在 Git Bash 里跑）
⚠️ **必须用 Git Bash**（开始菜单搜「Git Bash」），别用 CMD——
你之前把 Git Bash 命令粘到 CMD 会报语法错。

逐条复制下面命令，粘贴到 Git Bash 回车：

```bash
cd "/c/Users/Lenovo/WorkBuddy/2026-08-31-16-01-04/easybudget"
git remote add origin https://github.com/candy2jumping/anxinhua52.git
git push -u origin main
```

- 第一次 `push` 会**弹出浏览器窗口**让你登录 GitHub → 授权/输账号密码 → 登录后自动开始上传。
- 看到 `main -> main` 或 `To https://github.com/...` 之类的成功提示就 OK。

> 如果没弹窗、反而问你用户名/密码：说明 Git 凭证助手没生效。
> 解决：去 GitHub → Settings → Developer settings → Personal access tokens 建一个
> （勾 `repo` 权限），密码处粘贴这个 token 即可。（弄完可随手删掉 token，更安全。）

---

## 第 3 步：开启 GitHub Pages
1. 回到 github.com 的 `anxinhua52` 仓库
2. 点右上 **Settings** → 左侧 **Pages**
3. Source 选 **Deploy from a branch** → Branch 选 **main** → 目录选 **/root** → **Save**
4. 等 1~10 分钟，页面会显示你的网址：
   `https://candy2jumping.github.io/anxinhua52/`

---

## 第 4 步：手机上用
1. 手机浏览器打开 `https://candy2jumping.github.io/anxinhua52/`
2. 菜单「添加到主屏幕」→ 像 App 一样用
3. 验证：关掉电脑、甚至开飞行模式重开，应该还能开（数据在你手机本地）

---

## 以后更新代码怎么办
本地改完文件后，在 Git Bash 跑：

```bash
cd "/c/Users/Lenovo/WorkBuddy/2026-08-31-16-01-04/easybudget"
git add -A
git commit -m "写这次改了啥"
git push
```

GitHub Pages 会自动重新发布（约 1~2 分钟生效）。

---

## 数据说明（重要）
- 你的账本存在**手机本地**（IndexedDB），不在 GitHub、不在电脑。
- GitHub Pages 只托管「App 代码」，看不到你的任何记账内容。
- 仍建议偶尔在 App 里点「☰ → 数据备份与恢复 → 导出」存个 `.json` 备份。
- 换手机 / 清浏览器缓存前，务必先导出备份。
