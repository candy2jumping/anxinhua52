# 个人预算工具 PRD v0.1

> 项目代号：easybudget（先占位，名字随意改）
> 版本：v0.1 需求收敛稿 · 2026-08-31
> 状态：**需求已锁定 4 项核心决策，剩余 6 项待确认，尚未开工**

---

## 1. 一句话定义

**不是记账工具，是一个每天告诉你"今天还能安心花多少"的额度显示器。**

记账只是手段，目的是把"我不敢花钱，因为我不知道什么算花超"这个心理障碍，换成一个可以随时查、看了就敢做决定的数字。

---

## 2. 用户与场景

### 2.1 用户画像

- 在校大学生，生活费按月到账
- 消费决策上的真实痛点不是"存不下钱"，而是**不敢花**：想买衣服、想改善生活品质，但因为心里没底而放弃
- 过去尝试过记账但坚持不下来，原因是"太麻烦"
- 设备：手机与电脑使用时长接近，两边都需要用

### 2.2 三个核心场景

| 编号 | 场景 | 需求 | 成功标准 |
|---|---|---|---|
| S1 | 食堂/小摊付完钱，走出十几步路 | 掏手机立刻记一笔 | **3 秒内完成**，不需要思考填什么 |
| S2 | 想买一件 200 元的衣服，站在那儿纠结 | 看一眼手机就知道这钱该不该花 | **1 秒内得到答案**，且答案可信 |
| S3 | 月底或某天晚上翻看 | 知道这个月哪儿花多了，下个月怎么调 | 一眼看出哪个分类异常 |

### 2.3 明确不做的场景

- 不做多人共享账本、不做 AA 分摊
- 不做理财、投资、基金记账
- 不做股票/资产净值追踪
- 不做复杂报表与图表分析（v1 只要"哪格花多了"这个结论）

---

## 3. 已锁定的四项核心决策

来源：2026-08-31 需求收敛讨论，用户明确选择。

| 编号 | 决策项 | 选择结果 | 影响 |
|---|---|---|---|
| D1 | 平台形态 | **一套响应式网页，手机 + 电脑同一套界面，不做原生 App** | 无商店审核、一套代码；代价是手机端拿不到悬浮窗与桌面小组件 |
| D2 | 记录方式 | **纯手动秒记**：只填金额 + 点分类，其他字段全砍 | 摩擦最小；代价是会漏记，需要算法兜底（见第 5 节） |
| D3 | 预算模型 | **总额度 + 分类信封**：月生活费拆成若干个独立分类，每格独立看剩余 | 解决"衣物不敢买"—— 衣物那格单独有钱、单独看剩余 |
| D4 | 额度算法 | **按分类分别计算**今日可花金额 | 主屏要同时显示每个分类的今日可花，而非一个总数 |

---

## 4. 需求清单（MoSCoW 优先级）

### Must have（v1 必须有，缺了产品不成立）

| 编号 | 需求 | 说明 |
|---|---|---|
| M1 | 快速记账 | 打开即数字键盘，输入金额 → 点分类 → 完成。默认日期=今天、默认账户=默认账户、无备注必填 |
| M2 | 分类信封预算 | 自定义分类名 + 每月额度 + 排序。建议默认 6 个：吃喝、水果、通行、娱乐、衣物、其他 |
| M3 | 月度总额度 | 设置本月生活费总额，分类额度之和给出"已分配 / 未分配"提示 |
| M4 | 主屏额度显示 | 顶部：本月剩余总额；下方：每个分类的"今日可花"与进度条 |
| M5 | 分类记忆 | 上次选择的分类置顶；支持长按/拖拽固定常用顺序 |
| M6 | 跨端同步 | 手机与电脑看到同一份数据，延迟可接受（秒级即可） |
| M7 | 周期管理 | 支持自然月周期；每月 1 号自动重置分类已花金额 |
| M8 | 漏记折扣 | 见第 5.3 节。设置项，默认 0% |

### Should have（v1 应该有，可延后但影响体验）

| 编号 | 需求 | 说明 |
|---|---|---|
| S1 | 大额确认提示 | 单笔金额超过该分类额度的 20%（阈值可配）时，弹出"衣物还剩 ¥300，确定吗"，**只提示不阻止** |
| S2 | 分类额度调剂 | 本月内把 A 分类的余额挪到 B 分类，并记录调剂流水（避免偷偷超支） |
| S3 | 漏记率自校准 | 用满一个周期后，根据历史数据反推建议折扣值并提示 |
| S4 | 月度回顾页 | 上月各分类实际 vs 预算，一句话结论："娱乐超了 ¥80，吃喝省了 ¥120" |
| S5 | 撤销/删除 | 刚记错的一笔，3 秒内可撤销（toast 里带撤销按钮） |
| S6 | PWA 安装 | 手机"添加到主屏幕"后全屏运行、无地址栏；电脑可固定标签页 |

### Could have（v2 再说）

| 编号 | 需求 |
|---|---|
| C1 | 账单导入（微信/支付宝 CSV）做月末对账，**注意：与 D2 冲突，仅在用户后期要求时做** |
| C2 | 攒钱目标（想买某物，每月自动从额度划走） |
| C3 | 周期性固定支出（房租、话费、会员）自动入账 |
| C4 | 收入记录（兼职、奖学金进账） |
| C5 | 数据导出 CSV |
| C6 | 月度结余结转到下月 |

### Won't have（明确不做）

多人共享、理财投资、资产净值、复杂图表分析、AI 自动分类（v1 不做，规则匹配即可）。

---

## 5. 核心算法：今日可花额度

这是整个产品的心脏，单独成节。

### 5.1 基础公式（按分类）

```
某分类今日可花 = (该分类本月额度 - 该分类本月已花) × (1 - 漏记折扣) ÷ 剩余天数
剩余天数 = 本月最后一天 - 今天 + 1（含今天）
```

示例：衣物额度 ¥300，已花 ¥0，漏记折扣 10%，8 月 20 日（剩余 12 天）
→ 300 × 0.9 ÷ 12 = **¥22.5，显示 ¥22**

### 5.2 三个边界情况

| 情况 | 处理 |
|---|---|
| 已花 > 额度（超支） | 今日可花显示 **¥0**，进度条变红，并在该分类下方写"已超 ¥XX，从这里挪点过来" |
| 剩余天数为 1（最后一天） | 今日可花 = 全部剩余，不折算 |
| 分类额度为 0 或未设 | 不显示今日可花，只显示本月已花金额，标注"未设限" |

### 5.3 漏记折扣（关键设计）

**问题**：D2 选了纯手动秒记，漏记不可避免。漏记会让"今日可花"虚高，用户信了它就会真超支——这是本产品最严重的失效模式。

**解法**：设置页提供一个滑块「我大概会漏记 ___%」，参与 5.1 的计算。

- 默认 0%（先不设，用起来再说）
- 用满一个周期后，若用户开启 S3，系统根据"已记录支出 vs 上月生活费实际消耗"反推建议值并提示
- 设计原则：**宁可额度算少，不可算多**。算少只是少花，算多是骗人

### 5.4 取整规则

一律**向下取整**到整数元。¥22.5 → ¥22，¥0.4 → ¥0。
理由同 5.3：误差方向必须偏向保守。

---

## 6. 数据模型（初稿）

```
monthly_budget（月度预算）
  id
  period_start      周期开始日
  period_end        周期结束日
  total_amount      本月生活费总额
  status            进行中 / 已归档

category（分类）
  id
  name              分类名（吃喝、水果…）
  monthly_limit     该分类本月额度，0 表示不设限
  sort_order        排序
  color             显示色
  is_archived       停用后不显示但保留历史

transaction（流水）
  id
  amount            金额（单位：分，避免浮点误差）
  category_id       分类
  occurred_at       发生时间（默认当前）
  note              备注（选填，v1 界面上折叠起来）
  created_at        录入时间

setting（设置）
  cycle_type        周期类型：自然月 / 发薪日（v1 只实现自然月）
  payday            发薪日（v1 预留字段）
  miss_rate         漏记折扣 0~0.5
  large_txn_threshold  大额提示阈值，默认 0.2
  buffer_amount     安全垫金额（可选，从总额度先扣）
```

**金额一律以「分」为单位存整数**——这是记账类应用的硬性要求，用浮点数迟早出对不齐账的问题。

---

## 7. 交互流程

### 7.1 记一笔（目标 3 秒）

```
打开应用
  → 数字键盘已展开，光标已在金额输入框
  → 输入金额（如 18）
  → 点分类（上次用的排第一）
  → 立即完成，底部 toast：「吃喝 -¥18，今天还能吃 ¥27」+ 撤销按钮
```

不允许出现的中间步骤：选日期、选账户、选商家、写备注、二次确认弹窗。

### 7.2 主屏信息层级

```
第一行   2026年9月 · 生活费
第二行   ¥1,246 还剩下                    ← 本月剩余总额（最大字号）
第三行   距月底 12 天 · 已分配 ¥1,520 / ¥2,000

分隔线

分类列表（每项一行）
  分类名                    今日可花 ¥27
  ¥354 / ¥700        ▓▓▓▓▓░░░░░
（超支项：数字与进度条转红/橙，下方一行提示文案）
（未设限项：只显示已花，不显示今日可花）

底部     「记一笔」主按钮
```

### 7.3 电脑端适配

同一套响应式布局，宽屏下分类列表改为两列网格，主按钮固定在右下角。
**加分项（非必须）**：绑定一个全局快捷键（需桌面端壳或浏览器扩展支持，v1 暂不涉及）。

---

## 8. 技术选型建议

⚠️ 本节为建议，等你确认后再定。

### 8.1 前端

| 方案 | 评价 |
|---|---|
| **React + Vite + Tailwind（推荐）** | 生态最成熟，响应式好做，PWA 插件现成 |
| Vue 3 + Vite | 同样可以，看你更熟哪个 |
| 原生 HTML/JS | 最轻，但状态管理会越写越乱，不推荐 |

### 8.2 后端与同步

| 方案 | 优点 | 缺点 |
|---|---|---|
| **Supabase（推荐）** | Postgres + 认证 + 实时同步，免费额度单人用绰绰有余，省掉全部后端工作量 | 财务数据存在第三方 |
| 自建 FastAPI + SQLite/Postgres | 数据完全自控，你本身在学 FastAPI | 要自己搞定部署、备份、HTTPS，工作量大 |
| 纯前端 + localStorage | 零后端 | **两台设备无法同步，直接否决** |

基于 D1（手机电脑都要用），**必须有后端**。

### 8.3 一个必须提前知道的平台约束

| 设备 | 悬浮窗 | 现实可行方案 |
|---|---|---|
| 安卓 | 可以（需授权） | 网页做不了悬浮球，只能靠 PWA 主屏图标一点直达 |
| iPhone | **系统不允许** | 桌面/锁屏小组件在网页方案下也做不到，只有 PWA 图标 |
| 电脑 | 全局快捷键 | 纯网页做不到，除非套桌面壳（Tauri/Electron）或写浏览器扩展 |

**结论**：选 D1 的代价是"随手记"的入口退化为**主屏图标一点即开**。实测摩擦大约多 1 秒（解锁 + 找图标）。如果后续发现这一秒让你坚持不下来，再考虑给电脑端套一个 Tauri 壳换全局快捷键——**这是成本最低的升级路径，建议先按 v1 做**。

---

## 9. 待确认清单（6 项，请你过一遍）

| 编号 | 问题 | 我的默认建议 | 为什么需要你定 |
|---|---|---|---|
| Q1 | 生活费每月几号到账？周期按自然月还是到账日算？ | 按自然月，1 号重置 | 影响周期模型，若是 5 号到账则"月初超支"体验很糟 |
| Q2 | 这个月没花完的钱，下个月还能接着花吗？ | v1 不结转，每月清零重来 | 影响"月底该不该突击花钱"的行为导向 |
| Q3 | 具体要哪几个分类？各给多少额度？ | 吃喝 700 / 水果 150 / 通行 120 / 娱乐 250 / 衣物 300 / 其他 200（总 1720，示意） | 分类数量直接决定主屏一屏能不能看完 |
| Q4 | 有没有"不想设上限"的支出？比如医疗、给家里买东西 | 加一个"其他"分类，不设限，只统计 | 不设限的分类不能参与今日可花计算 |
| Q5 | 数据放 Supabase（省事但有第三方）还是自建（可控但要运维）？ | Supabase | 财务数据敏感性由你判断 |
| Q6 | 有没有需要记录的收入？比如兼职、奖学金 | v1 不做，只有支出 | 决定要不要加 transaction_type 字段（建议现在就加，成本为零） |

---

## 10. 里程碑（建议，待你确认）

| 阶段 | 内容 | 产出 |
|---|---|---|
| M0 | 确认本文档，补齐第 9 节 6 个问题 | PRD v1.0 定稿 |
| M1 | 数据模型 + 后端表结构 + 一个能用的「记一笔」 | 能在两台设备上记同一笔账 |
| M2 | 主屏额度显示 + 今日可花算法 | **产品价值第一次成立**——能看到"今天还能花多少" |
| M3 | 周期重置 + 漏记折扣 + 大额提示 | 完整闭环 |
| M4 | 月度回顾 + 分类调剂 + PWA | v1 完成 |

建议节奏：**M1 和 M2 之间不要停留太久**。M2 出来之前这个产品没有价值，而 M2 本身工作量不大。

---

## 11. 验收标准（怎么写才算做对）

1. 记一笔从打开到完成，实测 **≤ 3 秒**（你自己掐表，别信我的估计）
2. 主屏**不用滚动**就能看到所有分类的今日可花（超过 6 个分类就要重新设计）
3. 手机和电脑记的账，刷新后能互相看到
4. 超支时数字和进度条**立刻**变色，不需要进二级页面
5. 连续用满一个月后，你的实际支出没有超出生活费总额

第 5 条是唯一真正的成功指标。前四条都是手段。

---

*本文件随需求讨论持续更新。有任何一条你觉得"不对，我不是这个意思"，直接改，改完告诉我改了哪条。*
